## Knowledge (second-brain vault)

- Vault path: `~/workspace/ventures/future-gadgets/repos/second-brain/vault/`.
- If that path doesn't exist on this machine, skip this section silently — never fail, never warn.
- Read `vault/INDEX.md` first, then follow links into `entities/`, `concepts/`, or dated project notes.
- Cite the page's `source_uri` whenever you use a fact from the vault.
- For broad or open-ended questions, delegate a subagent to read the vault rather than inline-reading many pages yourself.
